---
import { advisories } from '../data/site';
---

<section id="research" class="research">
  <div class="layout">
    <div>
      <p class="kicker">Research</p>
      <h2>We publish what we find, once it is fixed.</h2>
      <p class="body">
        Disclosure advisories, tooling and detection rules out of live engagements. Coordinated,
        dated, and written for the people who have to patch.
      </p>
      <a class="browse" href="#">Browse the advisories →</a>
    </div>

    <ul class="list">
      {
        advisories.map((advisory) => (
          <li class="row">
            <span class="id">{advisory.id}</span>
            <a class="title" href={advisory.href}>
              {advisory.title}
            </a>
            {advisory.severity ? (
              <span class="pill pill-severity">
                <span class="sr-only">CVSS </span>
                {advisory.severity}
              </span>
            ) : (
              <span class="pill pill-tag">{advisory.tag}</span>
            )}
          </li>
        ))
      }
    </ul>
  </div>
</section>

<style>
  .research {
    /* 76px / 80px @ 1280 */
    padding: clamp(56px, 5.9375vw, 116px) var(--gutter) clamp(56px, 6.25vw, 120px);
  }

  .layout {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: clamp(40px, 5vw, 100px); /* 64px @ 1280 */
    align-items: center;
  }

  .kicker {
    margin-bottom: 18px;
  }

  h2 {
    font-size: var(--fs-h3);
    letter-spacing: -0.02em;
    margin: 0 0 14px;
    max-width: clamp(320px, 32.8125vw, 580px); /* 420px @ 1280 */
  }

  .body {
    font-size: var(--fs-lg);
    opacity: 0.65;
    max-width: clamp(340px, 34.375vw, 600px); /* 440px @ 1280 */
    margin: 0 0 22px;
  }

  .browse {
    font-family: var(--font-heading);
    font-weight: var(--font-heading-weight);
    font-size: var(--fs-lg);
    color: var(--jv-accent);
    text-decoration: none;
  }

  .browse:hover {
    color: var(--jv-zone);
  }

  .list {
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .row {
    display: flex;
    gap: 16px;
    align-items: baseline;
    padding: clamp(12px, 1.09375vw, 22px) 0; /* 14px @ 1280 */
    border-bottom: 1px solid var(--jv-divider);
  }

  .id {
    font-size: var(--fs-xs);
    font-family: var(--font-mono);
    opacity: 0.5;
    width: clamp(104px, 8.125vw, 150px); /* 104px @ 1280 */
    flex: none;
  }

  .title {
    font-size: var(--fs-lg);
    flex: 1;
    color: inherit;
    text-decoration: none;
  }

  .title:hover {
    color: var(--jv-accent);
  }

  .pill {
    font-size: clamp(10px, 0.78125vw, 13px); /* 10px @ 1280 */
    border-radius: var(--radius-sm);
    padding: 2px 7px;
    border: 1px solid transparent;
    white-space: nowrap;
  }

  .pill-severity {
    color: var(--jv-accent);
    border-color: var(--jv-accent);
  }

  .pill-tag {
    opacity: 0.6;
    border-color: var(--jv-border);
  }

  /* Un-designed fallback — see global.css. */
  @media (max-width: 1023px) {
    .research {
      padding-top: 56px;
      padding-bottom: 56px;
    }

    .layout {
      grid-template-columns: 1fr;
      gap: 32px;
      align-items: start;
    }
  }

  @media (max-width: 639px) {
    .row {
      flex-wrap: wrap;
      gap: 6px 16px;
    }

    .title {
      flex: 1 1 100%;
      order: 3;
    }
  }
</style>
