---
import LogoMark from './LogoMark.astro';
import ThemeToggle from './ThemeToggle.astro';
import { navLinks } from '../data/site';
---

<header class="nav">
  <a class="brand" href="/">
    <LogoMark size={30} />
    <span class="wordmark">Jovian<span class="wordmark-sub"> Security</span></span>
  </a>

  <nav class="links" aria-label="Primary">
    {navLinks.map((link) => <a href={link.href}>{link.label}</a>)}
  </nav>

  <ThemeToggle />

  <a class="btn btn-primary nav-cta" href="#contact">Request a scope call</a>
</header>

<style>
  .nav {
    display: flex;
    align-items: center;
    /* 28px / 20px @ 1280 */
    gap: clamp(20px, 2.1875vw, 42px);
    padding: clamp(16px, 1.5625vw, 30px) var(--gutter);
  }

  .brand {
    display: flex;
    align-items: center;
    gap: 11px;
    margin-right: auto;
    color: inherit;
    text-decoration: none;
  }

  .wordmark {
    font-family: var(--font-heading);
    font-weight: 500;
    font-size: var(--fs-2xl);
    letter-spacing: -0.01em;
  }

  .wordmark-sub {
    opacity: 0.5;
  }

  .links {
    display: flex;
    align-items: center;
    gap: clamp(20px, 2.1875vw, 42px); /* 28px @ 1280 */
  }

  .links a {
    color: inherit;
    text-decoration: none;
    font-size: var(--fs-lg);
    opacity: 0.7;
  }

  .links a:hover {
    opacity: 1;
  }

  .nav-cta {
    font-size: var(--fs-md);
    padding: clamp(7px, 0.546875vw, 11px) clamp(13px, 1.015625vw, 20px);
  }

  /* Un-designed fallback — see global.css. No mobile menu was designed, so the
     bar wraps to a second row rather than hiding the links behind a control
     that has no visual spec. */
  @media (max-width: 1023px) {
    .nav {
      flex-wrap: wrap;
      gap: 16px 20px;
    }

    .links {
      order: 3;
      flex-basis: 100%;
      flex-wrap: wrap;
      gap: 20px;
    }
  }
</style>
